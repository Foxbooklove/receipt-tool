# 사용자가 할 일

Claude가 코딩하는 동안 병렬로 진행할 것. 위에서부터 급한 순서다.
**키 값은 대화창에 붙여넣지 말고 아래 지정된 파일에 직접 넣는다.**

---

## 1. Supabase — 지금 필요

https://supabase.com 에서 프로젝트 생성. 리전은 `Northeast Asia (Seoul)`.

- [ ] **SQL 실행** — 대시보드 → `SQL Editor` → [supabase.sql](supabase.sql) 내용 전체 붙여넣고 Run
- [ ] **Storage 버킷 생성** — `Storage` → New bucket → 이름 `receipts` → **Public 체크**
- [ ] **URL·키 복사** — `Project Settings` → `API` → 아래 두 값

| 대시보드 항목 | 넣을 곳 |
|---|---|
| `Project URL` | [.env.local](.env.local) 의 `VITE_SUPABASE_URL` |
| `anon public` 키 | [.env.local](.env.local) 의 `VITE_SUPABASE_ANON_KEY` |

`.env.local`은 `.gitignore`에 있어 커밋되지 않는다. 따옴표 없이 값만 붙여넣는다.

---

## 2. GitHub 저장소 — 배포 전에 필요

- [ ] 빈 저장소 하나 생성 (private 무관). README 등 아무것도 추가하지 말 것
- [ ] 저장소 주소를 Claude에게 알려주면 `git init`부터 push까지 처리한다

`receipts/` 폴더는 `.gitignore`에 있으므로 영수증 이미지는 올라가지 않는다.

---

## 3. Vercel — GitHub 다음

https://vercel.com 에 GitHub 계정으로 가입.

- [ ] 위 저장소를 Import
- [ ] `Settings` → `Environment Variables` 에 아래 두 개 추가

| Name | Value |
|---|---|
| `VITE_SUPABASE_URL` | `.env.local`과 같은 값 |
| `VITE_SUPABASE_ANON_KEY` | `.env.local`과 같은 값 |

**이름이 한 글자라도 다르면 배포본에서 DB가 붙지 않는다.** 로컬 파일과 똑같이 적을 것.
환경변수를 나중에 추가했다면 재배포해야 반영된다.

---

## 4. Google Analytics — 6단계 증빙

https://analytics.google.com

- [ ] 속성 생성 → 데이터 스트림(웹) 추가
- [ ] **측정 ID** (`G-`로 시작) 를 Claude에게 알려주기 → `index.html`에 스니펫을 넣는다

사이트 URL은 나중에 바꿔도 되므로 지금은 아무거나 넣어도 된다.

---

## 5. cloudflared 설치 — 시연 때 필요

터미널에서 한 줄:

```powershell
winget install --id Cloudflare.cloudflared
```

지금 안 해도 되지만, 녹화 직전에 설치 문제로 막히는 것보다 미리 해두는 편이 낫다.

---

## 6. Search Console — 배포 후에만 가능

Vercel URL이 나와야 등록할 수 있다. **4번 작업(배포)이 끝난 뒤에 한다. 지금은 할 게 없다.**

- [ ] https://search.google.com/search-console 에서 URL 접두어로 등록
- [ ] 소유권 확인용 메타태그를 Claude에게 알려주기 → `index.html`에 넣고 재배포
- [ ] 색인 요청 제출 (반영은 느리므로 제출 화면까지만 영상에 담으면 충족)

---

## 7. 베이스라인 측정 — 개발 다 끝난 뒤

`receipts/answers.csv`에서 `split=eval`인 장들을 제3자에게 주고, 같은 컬럼 기준으로 엑셀에 수기 입력하게 한 뒤 시간을 잰다.

- [ ] 측정해 줄 사람 섭외
- [ ] 정답표를 미리 보여주지 않기
- [ ] 개발자 본인이 재지 않기 (수기 입력이 느리게 나오는 쪽으로 편향된다)
